AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: Serverless application with producer Lambda function

Parameters:
  ProjectName:
    Type: String
    Description: Name of the project
  FunctionName:
    Type: String
    Description: Name of the producer Lambda function
  LambdaExecutionRoleArn:
    Type: String
    Description: ARN of the existing IAM role for the producer Lambda execution
  
  QueueUrl:
    Type: AWS::SSM::Parameter::Value<String>
    Description: URL of the existing SQS queue
    Default: /sqs/oslo/queue-url
  AllowedIp:
    Type: AWS::SSM::Parameter::Value<String>
    Description: Comma-separated list of allowed IP addresses for the producer Lambda function
    Default: /allowed-ips/dedicated-ip
    NoEcho: true
  AllowedClientId:
    Type: AWS::SSM::Parameter::Value<String>
    Description: JWT sub claim value of the single allowed client
    Default: /projects/helsinki/allowed-client-id
    NoEcho: true

Outputs:
  FunctionArn:
    Description: ARN of the producer Lambda function
    Value: !GetAtt Function.Arn

  FunctionName:
    Description: Name of the producer Lambda function
    Value: !Ref FunctionName

Resources:
  ApiGateway:
    Type: AWS::Serverless::Api
    Properties:
      StageName: Prod
      EndpointConfiguration: REGIONAL
      Auth:
        ResourcePolicy:
          CustomStatements:
            - Effect: Deny
              Principal: "*"
              Action: execute-api:Invoke
              Resource: "arn:aws:execute-api:*:*:*/*/*/*"
              Condition:
                NotIpAddress:
                  aws:SourceIp: !Ref AllowedIp
  Function:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Ref FunctionName
      Runtime: python3.14
      Handler: app.main.lambda_handler
      CodeUri: lambda.zip
      Role: !Ref LambdaExecutionRoleArn
      Timeout: 10
      MemorySize: 128
      Environment:
        Variables:
          POWERTOOLS_SERVICE_NAME: !Ref FunctionName
          POWERTOOLS_LOG_LEVEL: INFO
          QUEUE_URL: !Ref QueueUrl
          ALLOWED_CLIENT_ID: !Ref AllowedClientId
          ALLOWED_CLIENT_SECRET: '{{resolve:ssm:/projects/helsinki/AllowedClientSecret:1}}'
          JWT_SIGNING_SECRET: '{{resolve:ssm:/projects/helsinki/JwtSigningSecret:1}}'
      Tags:
        Project: !Ref ProjectName
        Name: !Ref FunctionName
      Events:
        ApiEventRoot:
          Type: Api
          Properties:
            RestApiId: !Ref ApiGateway
            Path: /
            Method: ANY
        ApiEvent:
          Type: Api
          Properties:
            RestApiId: !Ref ApiGateway
            Path: /{proxy+}
            Method: ANY

  LogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub "/aws/lambda/${FunctionName}"
      RetentionInDays: 30
      Tags:
        - Key: Project
          Value: !Ref ProjectName
        - Key: Name
          Value: !Sub "/aws/lambda/${FunctionName}"

